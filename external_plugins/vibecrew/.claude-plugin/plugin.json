{
  "name": "vibecrew",
  "description": "Orchestrate a VibeCrew board from Claude Code, MCP-free, over its REST API via a bundled stdlib-only Python client. The orchestrator agent is HOST-TICKED — VibeCrew's runtime owns the loop and delivers each tick with a status digest; the agent replies with a CADENCE line and arms no cron of its own (see reference/tick-contract.md). Pipeline routing to Basic / Planned / Async with per-step agent + model bindings across Claude Code, OpenCode, Codex and Pi (Pi is an explicit-ask-only, uncalibrated arm; Codex reviews by default) via the classify-task skill, lanes (epic + sub-cards + blocking edges) for parallel dispatch, and pipeline overrides with merge-on delivery and post-plan gates. Ships the vibecrew, product-manager, classify-task, and answer-questions skills, the orchestrator (Claude + OpenCode twins) / product / planner / coder / decider agents, the pipeline-stage crew-bundle (vibecrew-product/-planner/-coder/-reviewer subagents + code-review-checklist skill — canonical source the app vendors), lifecycle prompts, and a standalone orchestrator launcher",
  "version": "0.5.0",
  "keywords": [
    "vibecrew",
    "kanban",
    "orchestration",
    "coding-agents",
    "rest-api",
    "product-manager",
    "sombrax"
  ]
}
